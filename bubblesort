#include<stdio.h>
int main()
{
int a[5]={1,2,3,4,5},i,j,temp;
for(i=;i<n-1;i++)
{
for(j=0;j<n-i-1;j++0
{
if(a[j]>a[j+1])
{
temp=a[j];
a[j]=a[j+1];
a[j+1]=temp;
}
}
}
printf("the final sorted array is");
for(i=0;i<n;i++)
printf("%d",a[i]);
}
